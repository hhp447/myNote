防止注入：（id=-1 or 1=1）

---输入变量检查
	1.如果传递的是id则判断是否数字。
	2.如果是帐号密码，正则校验preg_match("/^[a-zA-Z0-9]｛0，20｝$/",$name);


---转义
	3.过滤和转义特殊字符
		addslashes（）转义函数
		mysqli_real_escape_string()根据当前连接的字符集，对于 SQL 语句中的特殊字符进行转义

---mysql预编译机制
	4.占位符，预编译。